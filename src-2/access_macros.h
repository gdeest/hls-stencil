#ifndef ACCESS_MACROS_H
#define ACCESS_MACROS_H

#include "params.h"

/* #define AT_SIZE_UNSKEWED(arr,T,N0,N1,t,x0,x1) arr[(t+1)*((N0)*(N1))+(x0)*(N1)+(x1)] */
/* #define AT_SIZE_SKEWED(arr,T,N0,N1,t,x0,x1)   AT_SIZE_UNSKEWED(arr,T,N0,N1,t,(x0)-(t),(x1)-(t)) */
/* #define AT_TILED(arr,T,N0,N1,t,x0,x1,tt,xx0,xx1) AT_SIZE_SKEWED(arr,T,N0,N1,(t)*ST+(tt), (x0)*S0+(xx0), (x1)*S1+(xx1)) */

#endif
